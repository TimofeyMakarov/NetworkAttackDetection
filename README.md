NetworkAttackDetection
----------------------

🚀**Детектор сетевых атак с точностью обнаружения 99.7%** 🔥

Перейдя по ссылке Вы можете найти [ноутбук с обучением](https://colab.research.google.com/drive/19HVgpaWT9SLWWvfBy-g1t9P1rcoHKkhO?usp=sharing) ML-модели. Репозиторий представляет собой C++ проект. Структура репозитория:

```
NetworkAttackDetection/
├── data/
│   └── NetworkAtackDetection_Data.csv
├── include/
│   └── ONNXModel.h
├── models/
│   └── DecisionTree_CICIDS2017_NetworkAttackDetector_v1.onnx
├── src/
│   ├── ONNXModel.cpp
│   └── main.cpp
├── .gitignore
├── CMakeLists.txt
└── README.md
```

## Инструкция по запуску проекта

## Выбор датасета
## Выбор алгоритма
   Для задачи детекции сетевых атак в реальном времени я выбрал алгоритм Decision Tree по следующим причинам:

   - **Скорость инференса** оценивается как O(logN), где N - кол-во вершин в дереве. В коде ниже значение logN ограничивается сверху параметром max_depth, который по умолчанию равен 10, что позволяет предотвратить переобучение модели и гарантирует быстрый отклик. Это очень высокая скорость и подходит для real-time.
   - **Экономичное использование памяти** обуславливается хранением только структуры дерева (с учётом ограничения max_depth = 10, кол-во вершин не превосходит 1024, а в среднем это кол-во всегда меньше и ожидается приблизительно 150 вершин).
   - **Интерпретируемость модели** на основе дерева решений позволяет анализировать, какие признаки наиболее важны для детекции атак.
   - **Точность модели**, обученной данным алгоритмом, как покажет описание ниже, очень высока и достигает 99.7%
    
## Выбор метрик
